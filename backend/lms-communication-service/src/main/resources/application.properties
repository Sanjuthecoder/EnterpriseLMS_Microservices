server.port=8085
spring.application.name=lms-communication-service

# Eureka
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka/
eureka.instance.preferIpAddress=true

# Redis
spring.data.redis.host=localhost
spring.data.redis.port=6379

# MongoDB (Global Cluster)
spring.data.mongodb.uri=${DB_MONGO_URI:mongodb+srv://sanjaysharmajr07_db_user:2qhwUbhjsWNykJK4@enterprisecluster.gtynvaw.mongodb.net/enterprise_db?appName=enterpriseCluster}

# Actuator Observability
management.endpoints.web.exposure.include=health,metrics,prometheus
management.endpoint.health.show-details=always
